{% extends 'WalvaHafBundle:Categorie:admin.html.twig' %}

{% block breadcrumb %}
{{ parent() }}
<li class="active">Show</li>
{% endblock %}

{% block body -%}
    <h1>Categorie</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nomfr</th>
                <td>{{ entity.nomFr }}</td>
            </tr>
            <tr>
                <th>Nomnl</th>
                <td>{{ entity.nomNl }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}

    
    {% block container_header_right %}

        <div class="btn-group">
            <a class="btn " href="{{ path('categorie') }}">
                <i class="icon-chevron-left"></i> 
                Back to the list
            </a>
            <a class="btn " href="{{ path('categorie_edit', { 'id': entity.id }) }}">
                <i class="icon-pencil"></i> Edit</a>
        </a>

    </div>
{% endblock %}