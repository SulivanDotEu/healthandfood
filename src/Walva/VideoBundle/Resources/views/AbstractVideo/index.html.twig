{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AbstractVideo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Internalname</th>
                <th>Audiolanguage</th>
                <th>Subtitleslanguage</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('abstractvideo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.internalName }}</td>
                <td>{{ entity.audioLanguage }}</td>
                <td>{{ entity.subtitlesLanguage }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('abstractvideo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('abstractvideo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('abstractvideo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
